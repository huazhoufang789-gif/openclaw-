# 🚀 OpenClaw更新监控系统 - 真正的一键部署

## 📋 只需一条命令

### 方法1：使用GitHub原始文件（推荐）
```bash
# 复制粘贴这一条命令，等待完成
curl -sL https://raw.githubusercontent.com/huazhoufang789-gif/openclaw-/main/one_click_deploy.sh | bash
```

### 方法2：使用备用URL
```bash
# 如果上面的命令失败，尝试这个
curl -sL https://cdn.jsdelivr.net/gh/huazhoufang789-gif/openclaw-/one_click_deploy.sh | bash
```

### 方法3：下载后运行
```bash
# 1. 下载脚本
wget https://raw.githubusercontent.com/huazhoufang789-gif/openclaw-/main/one_click_deploy.sh

# 2. 运行脚本
chmod +x one_click_deploy.sh
./one_click_deploy.sh
```

## 🎯 一键部署功能

### 自动完成以下所有步骤：
1. ✅ **环境检查** - 检测系统、Python、Git
2. ✅ **网络检测** - 自动测试网络连接，提示配置代理
3. ✅ **端口检查** - 自动检测端口占用，选择可用端口
4. ✅ **项目获取** - 自动克隆GitHub仓库
5. ✅ **依赖安装** - 自动安装Python依赖
6. ✅ **配置生成** - 自动创建配置文件
7. ✅ **OpenClaw集成** - 自动检测并集成OpenClaw
8. ✅ **快捷脚本** - 创建运行、测试、定时任务脚本
9. ✅ **初始测试** - 自动运行快速测试
10. ✅ **部署报告** - 生成完整的部署摘要

## ⚡ 部署过程预览

### 部署开始
```
╔══════════════════════════════════════════════════════╗
║      OpenClaw更新监控系统 - 一键部署                ║
║      自动部署Twitter/GitHub监控 + 小红书生成         ║
╚══════════════════════════════════════════════════════╝
```

### 自动检测和配置
```
▶ 阶段1：环境检查
✓ 操作系统: Ubuntu 22.04
✓ Python3: 3.10.12
✓ Git: 2.34.1

▶ 阶段2：网络配置
✓ 网络连接正常

▶ 阶段3：端口检查
✓ 网关端口 8888 可用
✓ Web端口 4000 可用
```

### 自动安装和配置
```
▶ 阶段4：获取项目代码
✓ 项目克隆成功: /home/user/openclaw-monitor-202602260329

▶ 阶段5：安装Python依赖
✓ Python依赖安装成功

▶ 阶段6：项目配置
✓ 配置文件已创建
✓ 目录结构已创建
```

### 完成部署
```
🎉 部署完成！

📊 部署摘要:
  - 部署时间: 45秒
  - 项目目录: /home/user/openclaw-monitor-202602260329
  - 网关端口: 8888
  - Web端口: 4000

🚀 可用命令:
  cd /home/user/openclaw-monitor-202602260329
  ./run.sh          # 运行完整监控
  ./test.sh         # 快速测试
  ./setup_cron.sh   # 设置定时任务
```

## 🔧 部署后操作

### 1. 进入项目目录
```bash
# 脚本会显示项目目录，如：
cd /home/user/openclaw-monitor-202602260329
```

### 2. 运行完整监控
```bash
./run.sh
```

### 3. 快速测试
```bash
./test.sh
```

### 4. 设置定时任务
```bash
./setup_cron.sh
```

### 5. 查看输出
```bash
ls -la output/
cat output/xiaohongshu/*.txt
```

## 🌐 网络要求

### 必须能访问：
- ✅ `https://api.github.com` - GitHub API
- ✅ `https://api.fxtwitter.com` - Twitter API
- ✅ `https://raw.githubusercontent.com` - 脚本下载

### 代理支持：
如果网络需要代理，部署过程中会提示输入：
```
是否需要配置代理？(y/N): y
请输入代理地址（格式: http://proxy.example.com:8080）: http://your-proxy:8080
```

## ⚠️ 注意事项

### 1. 权限要求
- 需要`sudo`权限安装系统包
- 需要网络访问权限
- 需要写入当前目录权限

### 2. 系统要求
- **Linux** (Ubuntu/Debian/CentOS/RHEL)
- **macOS** (需要Homebrew)
- **Windows** (需要WSL2)

### 3. 时间估算
- 网络正常：1-2分钟
- 需要安装依赖：3-5分钟
- 首次运行：额外1-2分钟

## 🔍 故障排除

### 问题1：curl命令失败
```bash
# 尝试使用wget
wget -O - https://raw.githubusercontent.com/huazhoufang789-gif/openclaw-/main/one_click_deploy.sh | bash
```

### 问题2：网络连接失败
```bash
# 手动设置代理后运行
export HTTP_PROXY="http://proxy:8080"
export HTTPS_PROXY="http://proxy:8080"
curl -sL https://raw.githubusercontent.com/huazhoufang789-gif/openclaw-/main/one_click_deploy.sh | bash
```

### 问题3：权限不足
```bash
# 使用sudo运行
curl -sL https://raw.githubusercontent.com/huazhoufang789-gif/openclaw-/main/one_click_deploy.sh | sudo bash
```

### 问题4：Python安装失败
```bash
# 手动安装Python后重新运行
sudo apt-get update && sudo apt-get install -y python3 python3-pip
curl -sL https://raw.githubusercontent.com/huazhoufang789-gif/openclaw-/main/one_click_deploy.sh | bash
```

## 📁 生成的文件

### 项目目录结构
```
openclaw-monitor-202602260329/
├── run.sh                    # 运行脚本
├── test.sh                   # 测试脚本
├── setup_cron.sh             # 定时任务设置
├── setup_openclaw.sh         # OpenClaw集成
├── config/
│   └── config.yaml          # 配置文件
├── scripts/                  # Python脚本
├── output/                   # 输出文件
├── logs/                     # 日志文件
└── skills/                   # OpenClaw技能
```

### 配置文件示例
```yaml
# 自动生成的配置
twitter:
  enable: true
  tweet_id: "2026503611514069173"
  translate: true

github:
  enable: true
  repo: "openclaw/openclaw"
  translate: true

xiaohongshu:
  enable: true
  include_links: false
```

## 🎯 验证部署成功

### 运行验证命令
```bash
# 进入项目目录后运行
./test.sh

# 应该看到：
# 🧪 开始快速测试...
# 1. 测试Twitter监控... ✓
# 2. 测试GitHub监控... ✓
# 3. 测试小红书内容生成... ✓
# ✅ 测试完成
```

### 检查输出文件
```bash
ls -la output/
# 应该看到：
# twitter/    github/    xiaohongshu/
```

## 🔄 更新和卸载

### 更新项目
```bash
# 进入项目目录
cd /path/to/project

# 拉取最新代码
git pull origin main

# 更新依赖
pip3 install -r requirements.txt --upgrade
```

### 重新部署
```bash
# 直接运行一键部署脚本，会自动处理更新
curl -sL https://raw.githubusercontent.com/huazhoufang789-gif/openclaw-/main/one_click_deploy.sh | bash
```

### 卸载（如果需要）
```bash
# 删除项目目录
rm -rf /path/to/openclaw-monitor-*

# 删除定时任务
crontab -l | grep -v "openclaw-monitor" | crontab -

# 删除环境变量
sed -i '/OPENCLAW_WORKSPACE/d' ~/.bashrc
sed -i '/OPENCLAW_CONFIG/d' ~/.bashrc
```

## 📞 获取帮助

### 查看部署日志
```bash
# 查看部署过程日志
cat ~/openclaw-deploy.log  # 如果脚本创建了日志
```

### 查看项目文档
```bash
# 进入项目目录查看
cd /path/to/project
cat README.md
cat 精准部署指南.md
```

### 问题反馈
1. 检查日志文件：`logs/` 目录
2. 运行调试模式：`./test.sh 2>&1 | tee debug.log`
3. 查看GitHub Issues

## 🎉 开始部署！

**现在只需复制粘贴这一条命令：**

```bash
curl -sL https://raw.githubusercontent.com/huazhoufang789-gif/openclaw-/main/one_click_deploy.sh | bash
```

**然后：**
1. 等待部署完成（1-5分钟）
2. 按照屏幕提示操作
3. 运行 `./run.sh` 开始使用

**一切都会自动完成！** 🚀