# 🚀 OpenClaw更新监控系统 - 新环境快速部署指南

## 📋 部署前检查清单

### **必需条件**
- [ ] Linux/macOS系统（Windows需要WSL）
- [ ] Python 3.7+
- [ ] Git
- [ ] 网络访问（GitHub、Twitter API、GitHub API）

### **可选条件**
- [ ] OpenClaw已安装
- [ ] 代理配置（如果需要）
- [ ] 定时任务权限（cron）

## ⚡ 5分钟快速部署

### **步骤1：基础环境**
```bash
# 安装基础工具
sudo apt-get update
sudo apt-get install -y git python3 python3-pip

# 或使用一键脚本
curl -s https://raw.githubusercontent.com/huazhoufang789-gif/openclaw-/main/deploy.sh | bash
```

### **步骤2：获取项目**
```bash
# 克隆项目
git clone git@github.com:huazhoufang789-gif/openclaw-.git
cd openclaw-

# 或下载压缩包
wget https://github.com/huazhoufang789-gif/openclaw-/archive/refs/heads/main.zip
unzip main.zip
cd openclaw--main
```

### **步骤3：安装依赖**
```bash
# 安装Python依赖
pip3 install requests pyyaml

# 验证安装
python3 -c "import requests, yaml; print('✓ 依赖安装成功')"
```

### **步骤4：配置项目**
```bash
# 复制配置文件
cp config/config.example.yaml config/config.yaml

# 编辑配置文件（可选）
vim config/config.yaml

# 创建输出目录
mkdir -p output/{twitter,github,xiaohongshu} logs
```

### **步骤5：测试运行**
```bash
# 快速测试
python3 scripts/pipeline.py --all

# 或分步测试
python3 scripts/twitter_monitor.py --tweet-id 2026503611514069173
python3 scripts/github_monitor.py --latest
python3 scripts/xiaohongshu_generator.py --help
```

## 🔧 端口配置方案

### **方案A：标准端口（默认）**
```bash
# OpenClaw默认端口
# - Gateway: 8080
# - WebChat: 3000

# 无需特殊配置
openclaw gateway start
```

### **方案B：自定义端口**
```bash
# 1. 修改OpenClaw配置
openclaw config set gateway.port 8888
openclaw config set webchat.port 4000

# 2. 重启服务
openclaw gateway restart

# 3. 更新项目配置
echo "OPENCLAW_GATEWAY_URL=http://localhost:8888" >> .env
```

### **方案C：Docker容器**
```bash
# 使用特定端口运行
docker run -p 8888:8080 -p 4000:3000 \
  -v $(pwd):/workspace \
  openclaw/openclaw:latest

# 在容器内运行监控
docker exec -it container-name bash
cd /workspace && python3 scripts/pipeline.py --all
```

## 🌐 代理配置指南

### **如果网络需要代理：**

#### **1. 环境变量代理**
```bash
# 临时设置
export HTTP_PROXY="http://proxy.example.com:8080"
export HTTPS_PROXY="http://proxy.example.com:8080"

# 永久设置（添加到 ~/.bashrc）
echo 'export HTTP_PROXY="http://proxy.example.com:8080"' >> ~/.bashrc
echo 'export HTTPS_PROXY="http://proxy.example.com:8080"' >> ~/.bashrc
source ~/.bashrc
```

#### **2. Git代理**
```bash
# 设置Git代理
git config --global http.proxy http://proxy.example.com:8080
git config --global https.proxy http://proxy.example.com:8080

# 取消代理
git config --global --unset http.proxy
git config --global --unset https.proxy
```

#### **3. Python代理**
```bash
# 在代码中设置
import requests
proxies = {
    'http': 'http://proxy.example.com:8080',
    'https': 'http://proxy.example.com:8080'
}
response = requests.get(url, proxies=proxies)
```

## 🛠️ OpenClaw集成

### **如果已安装OpenClaw：**

#### **1. 安装技能**
```bash
# 进入项目目录
cd openclaw-update-monitor

# 安装小红书发布助手技能
openclaw skills install ./skills/xiaohongshu-poster

# 验证安装
openclaw skills list | grep xiaohongshu
```

#### **2. 配置环境**
```bash
# 设置工作空间
export OPENCLAW_WORKSPACE=$(pwd)

# 设置配置文件
export OPENCLAW_CONFIG=$(pwd)/config/config.yaml

# 添加到bashrc（永久）
echo "export OPENCLAW_WORKSPACE=$(pwd)" >> ~/.bashrc
echo "export OPENCLAW_CONFIG=$(pwd)/config/config.yaml" >> ~/.bashrc
```

#### **3. 使用技能**
```bash
# 生成小红书内容
openclaw run --prompt "使用小红书发布助手生成OpenClaw v2026.2.24更新内容"

# 运行完整监控
openclaw run --prompt "运行OpenClaw更新监控流水线"
```

## ⏰ 自动化部署

### **一键部署脚本**
```bash
# 下载并运行部署脚本
curl -s https://raw.githubusercontent.com/huazhoufang789-gif/openclaw-/main/deploy_new_environment.sh | bash

# 或本地运行
chmod +x deploy_new_environment.sh
./deploy_new_environment.sh
```

### **Docker部署**
```bash
# 创建Dockerfile
cat > Dockerfile << EOF
FROM python:3.9-slim

WORKDIR /app

# 安装依赖
RUN apt-get update && apt-get install -y git
RUN pip install requests pyyaml

# 克隆项目
RUN git clone https://github.com/huazhoufang789-gif/openclaw-.git .

# 创建启动脚本
COPY entrypoint.sh /app/
RUN chmod +x /app/entrypoint.sh

ENTRYPOINT ["/app/entrypoint.sh"]
EOF

# 构建并运行
docker build -t openclaw-monitor .
docker run -d --name monitor openclaw-monitor
```

## 🔍 故障排除

### **常见问题1：网络连接失败**
```bash
# 测试网络连接
curl -I https://api.github.com
curl -I https://api.fxtwitter.com

# 如果失败，检查代理
echo $HTTP_PROXY
echo $HTTPS_PROXY
```

### **常见问题2：Python依赖错误**
```bash
# 升级pip
pip3 install --upgrade pip

# 重新安装依赖
pip3 uninstall requests pyyaml -y
pip3 install requests pyyaml

# 使用虚拟环境
python3 -m venv venv
source venv/bin/activate
pip install requests pyyaml
```

### **常见问题3：Git权限错误**
```bash
# 检查SSH密钥
ssh -T git@github.com

# 如果失败，使用HTTPS
git remote set-url origin https://github.com/huazhoufang789-gif/openclaw-.git
git pull
```

### **常见问题4：OpenClaw端口冲突**
```bash
# 检查端口占用
sudo netstat -tulpn | grep :8080
sudo netstat -tulpn | grep :3000

# 修改端口
openclaw config set gateway.port 8888
openclaw gateway restart
```

## 📊 验证部署

### **验证步骤**
```bash
# 1. 检查文件结构
ls -la scripts/ config/ skills/

# 2. 测试脚本
python3 scripts/pipeline.py --help

# 3. 运行测试
python3 scripts/twitter_monitor.py --tweet-id 2026503611514069173 --translate

# 4. 检查输出
ls -la output/twitter/ output/github/

# 5. 验证OpenClaw集成（如果安装）
openclaw skills list | grep xiaohongshu
```

### **成功标志**
- ✅ 项目文件完整
- ✅ Python依赖安装成功
- ✅ 网络连接正常
- ✅ 脚本运行无错误
- ✅ 输出文件生成正常

## 🎯 后续操作

### **1. 配置定时任务**
```bash
# 每天上午9点运行
echo "0 9 * * * cd $(pwd) && python3 scripts/pipeline.py --all >> logs/cron.log 2>&1" | crontab -

# 查看定时任务
crontab -l
```

### **2. 监控日志**
```bash
# 实时查看日志
tail -f logs/cron.log

# 查看错误日志
grep -i error logs/*.log
```

### **3. 更新项目**
```bash
# 拉取最新代码
git pull origin main

# 更新依赖
pip3 install -r requirements.txt --upgrade
```

## 📞 获取帮助

### **文档资源**
- **项目README**: https://github.com/huazhoufang789-gif/openclaw-
- **OpenClaw文档**: https://docs.openclaw.ai
- **Python requests文档**: https://docs.python-requests.org

### **问题反馈**
1. 检查日志文件：`logs/` 目录
2. 运行测试：`python3 scripts/pipeline.py --debug`
3. 查看GitHub Issues

### **社区支持**
- OpenClaw Discord: https://discord.com/invite/clawd
- GitHub Discussions

---
*部署指南版本: v1.0*
*最后更新: 2026-02-26*