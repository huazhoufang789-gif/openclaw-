# 🔧 OpenClaw精准部署指南（无冗余版）

## 🎯 部署目标
在新环境中快速、精准部署OpenClaw更新监控系统，避免任何无用功。

## 📋 前置检查（5分钟，避免后续弯路）

### 1. 网络环境确认
```bash
# 测试关键API访问（必须能访问）
curl -s -o /dev/null -w "%{http_code}" https://api.github.com
curl -s -o /dev/null -w "%{http_code}" https://api.fxtwitter.com

# 结果判断：
# - 200: 网络正常，无需代理
# - 其他: 需要代理，准备代理地址
```

**代理地址格式**（如果需要）：
```
http://proxy.example.com:8080
```

### 2. 端口规划确认
```bash
# 检查默认端口占用
netstat -tulpn | grep -E "(:8080|:3000)"

# 如果占用，选择替代端口：
# - Gateway端口：8080, 8888, 8081, 8088
# - WebChat端口：3000, 4000, 3001, 3008
```

**端口规划示例**：
```
网关端口: 8888
Web界面端口: 4000
```

## 🚀 阶段1：极简环境初始化（3分钟）

### 1.1 仅安装必需工具
```bash
# 更新包列表（仅基础）
sudo apt-get update -y

# 安装核心工具（仅必需）
sudo apt-get install -y \
  git \
  python3 \
  python3-pip \
  curl \
  wget

# 验证安装
git --version
python3 --version
pip3 --version
```

### 1.2 按需配置代理
```bash
# 仅当需要代理时执行
if [ "$NEEDS_PROXY" = "true" ]; then
    export HTTP_PROXY="http://proxy.example.com:8080"
    export HTTPS_PROXY="http://proxy.example.com:8080"
    
    # Git代理（仅克隆时需要）
    git config --global http.proxy "$HTTP_PROXY"
    git config --global https.proxy "$HTTPS_PROXY"
    
    echo "代理已配置"
else
    echo "跳过代理配置"
fi
```

### 1.3 安装Python核心依赖
```bash
# 仅安装项目必需的包
pip3 install \
  requests \
  pyyaml \
  --no-cache-dir \
  --quiet

# 验证依赖
python3 -c "import requests, yaml; print('✓ 依赖安装成功')"
```

## 📁 阶段2：项目代码与配置（2分钟）

### 2.1 克隆项目
```bash
# 克隆目标仓库（无多余操作）
git clone git@github.com:huazhoufang789-gif/openclaw-.git openclaw-monitor
cd openclaw-monitor

# 验证克隆
ls -la scripts/ config/
```

### 2.2 精准配置文件
```bash
# 复制模板
cp config/config.example.yaml config/config.yaml

# 仅修改核心配置项
cat > config/config.yaml << 'EOF'
# OpenClaw更新监控核心配置
# 仅修改以下项，其他保持默认

twitter:
  enable: true
  tweet_id: "2026503611514069173"
  output_dir: "./output/twitter"
  translate: true

github:
  enable: true
  repo: "openclaw/openclaw"
  output_dir: "./output/github"
  translate: true

xiaohongshu:
  enable: true
  output_dir: "./output/xiaohongshu"
  include_links: false  # 小红书不允许外部链接

# 网络配置（仅当需要代理时取消注释）
# network:
#   proxy: "http://proxy.example.com:8080"
#   timeout: 30
EOF

# 创建输出目录
mkdir -p output/{twitter,github,xiaohongshu} logs
```

## 🔌 阶段3：OpenClaw核心集成（3分钟）

### 3.1 安装目标技能
```bash
# 仅安装小红书海报技能
openclaw skills install ./skills/xiaohongshu-poster

# 验证安装
openclaw skills list | grep xiaohongshu
# 输出应包含: xiaohongshu-poster
```

### 3.2 配置环境变量
```bash
# 仅设置必需的环境变量
export OPENCLAW_WORKSPACE=$(pwd)
export OPENCLAW_CONFIG=$(pwd)/config/config.yaml

# 永久生效（可选）
echo "export OPENCLAW_WORKSPACE=$(pwd)" >> ~/.bashrc
echo "export OPENCLAW_CONFIG=$(pwd)/config/config.yaml" >> ~/.bashrc
source ~/.bashrc
```

### 3.3 让OpenClaw听懂你的话
```bash
# 1. 配置OpenClaw理解上下文
openclaw config set agent.context.enabled true
openclaw config set agent.context.memory true

# 2. 设置技能别名（便于调用）
openclaw config set skills.aliases.xhs "xiaohongshu-poster"

# 3. 验证配置
openclaw config show | grep -E "(context|skills)"
```

## 🌐 阶段4：联网功能配置（2分钟）

### 4.1 配置OpenClaw联网
```bash
# 1. 启用网络访问
openclaw config set network.enabled true
openclaw config set network.allow_external true

# 2. 配置代理（仅当需要时）
if [ "$NEEDS_PROXY" = "true" ]; then
    openclaw config set network.proxy.http "$HTTP_PROXY"
    openclaw config set network.proxy.https "$HTTPS_PROXY"
fi

# 3. 允许访问的域名
openclaw config set network.allowed_domains \
  "api.github.com,api.fxtwitter.com,github.com,raw.githubusercontent.com"
```

### 4.2 连接ClawHub下载技能
```bash
# 1. 配置ClawHub源
openclaw config set skills.sources.clawhub.enabled true
openclaw config set skills.sources.clawhub.url "https://clawhub.com"

# 2. 测试ClawHub连接
openclaw skills search "twitter" --source clawhub

# 3. 从ClawHub安装技能（如果需要）
# openclaw skills install clawhub:twitter-monitor
```

### 4.3 验证联网功能
```bash
# 测试网络访问
openclaw run --prompt "访问 https://api.github.com 并返回状态码"

# 测试技能下载
openclaw skills list --source clawhub
```

## 🔧 阶段5：端口配置与服务启动（2分钟）

### 5.1 精准端口配置
```bash
# 使用你规划的端口
GATEWAY_PORT=8888
WEBCHAT_PORT=4000

# 配置端口
openclaw config set gateway.port $GATEWAY_PORT
openclaw config set webchat.port $WEBCHAT_PORT

# 验证配置
openclaw config show | grep -E "port"
```

### 5.2 启动服务
```bash
# 重启网关服务
openclaw gateway restart

# 等待服务启动
sleep 3

# 验证端口监听
netstat -tulpn | grep -E "(:$GATEWAY_PORT|:$WEBCHAT_PORT)"
# 输出应有openclaw进程
```

### 5.3 访问Web界面
```bash
echo "OpenClaw服务已启动："
echo "- 网关地址: http://localhost:$GATEWAY_PORT"
echo "- Web界面: http://localhost:$WEBCHAT_PORT"
```

## ✅ 阶段6：最小化验证（2分钟）

### 6.1 测试核心流水线
```bash
# 仅运行核心功能测试
python3 scripts/pipeline.py \
  --twitter-only \
  --translate \
  --no-summary

# 验证输出
ls -la output/twitter/*.json | head -3
```

### 6.2 测试OpenClaw技能
```bash
# 测试小红书技能
openclaw run --prompt "使用小红书发布助手生成OpenClaw更新内容"

# 测试完整流水线
openclaw run --prompt "运行OpenClaw更新监控，获取最新版本信息并生成小红书内容"
```

### 6.3 验证联网功能
```bash
# 测试外部API访问
openclaw run --prompt "获取GitHub上openclaw/openclaw仓库的最新发布信息"

# 测试ClawHub访问
openclaw run --prompt "搜索ClawHub上关于Twitter监控的技能"
```

## 🎯 精准使用指南

### 如何让OpenClaw听懂你的话

#### 1. 使用明确指令
```bash
# ❌ 模糊
"帮我处理一下Twitter"

# ✅ 精准
"使用twitter_monitor技能获取推文ID 2026503611514069173的内容并翻译"
```

#### 2. 指定技能和参数
```bash
# 调用特定技能
openclaw run --prompt "使用技能 xiaohongshu-poster 生成内容"

# 传递参数
openclaw run --prompt "运行pipeline.py --all --translate"
```

#### 3. 使用上下文记忆
```bash
# 启用上下文
openclaw config set agent.context.enabled true

# 后续对话会记住上下文
openclaw run --prompt "获取最新推文"
openclaw run --prompt "把刚才的推文翻译成中文"
openclaw run --prompt "生成小红书内容"
```

### 联网功能详解

#### 1. 网络访问配置
```bash
# 查看当前网络配置
openclaw config show network

# 临时启用网络（单次会话）
openclaw run --network-enabled true --prompt "访问网页"

# 永久启用
openclaw config set network.enabled true
```

#### 2. 代理配置
```bash
# 设置代理
openclaw config set network.proxy.http "http://proxy:8080"
openclaw config set network.proxy.https "http://proxy:8080"

# 测试代理
openclaw run --prompt "使用代理访问 https://api.github.com"
```

#### 3. 访问ClawHub
```bash
# 搜索技能
openclaw skills search "监控" --source clawhub

# 安装技能
openclaw skills install clawhub:skill-name

# 更新技能列表
openclaw skills update --source clawhub
```

### 常用命令速查

#### 部署相关
```bash
# 检查服务状态
openclaw gateway status

# 查看日志
openclaw logs --follow

# 重启服务
openclaw gateway restart
```

#### 技能管理
```bash
# 列出所有技能
openclaw skills list

# 搜索技能
openclaw skills search "关键词"

# 安装技能
openclaw skills install <技能路径或URL>

# 卸载技能
openclaw skills uninstall <技能名>
```

#### 运行任务
```bash
# 运行Python脚本
openclaw run --prompt "执行 scripts/pipeline.py"

# 使用特定技能
openclaw run --skill xiaohongshu-poster --prompt "生成内容"

# 带参数运行
openclaw run --prompt "运行监控，参数：--all --translate"
```

## ⚠️ 避坑关键点

### 1. 网络问题
```bash
# 诊断网络
curl -I https://api.github.com
curl -I https://api.fxtwitter.com

# 如果失败，检查：
# 1. 代理配置是否正确
# 2. 防火墙是否允许
# 3. DNS解析是否正常
```

### 2. 端口冲突
```bash
# 提前检查
netstat -tulpn | grep -E "(:8080|:3000|:8888|:4000)"

# 解决方案：
# 1. 停止占用进程
# 2. 使用其他端口
# 3. 修改OpenClaw配置
```

### 3. 权限问题
```bash
# 检查权限
ls -la ~/.openclaw/
ls -la /var/run/openclaw/

# 解决方案：
# 1. 使用sudo（谨慎）
# 2. 修改目录权限
# 3. 使用正确的用户运行
```

### 4. 技能加载失败
```bash
# 检查技能
openclaw skills list --verbose

# 解决方案：
# 1. 重新安装技能
# 2. 检查技能路径
# 3. 查看技能日志
```

## 📊 部署验证清单

### 环境验证
- [ ] Python 3.7+ 已安装
- [ ] Git 已安装
- [ ] 网络访问正常
- [ ] 端口无冲突

### 项目验证
- [ ] 项目克隆成功
- [ ] 配置文件就绪
- [ ] 输出目录创建
- [ ] 依赖安装完成

### OpenClaw验证
- [ ] 服务启动正常
- [ ] 技能安装成功
- [ ] 网络配置正确
- [ ] 端口监听正常

### 功能验证
- [ ] Twitter监控可用
- [ ] GitHub监控可用
- [ ] 小红书生成可用
- [ ] 流水线运行正常

## 🔄 维护与更新

### 定期维护
```bash
# 更新项目代码
cd openclaw-monitor && git pull

# 更新Python依赖
pip3 install -r requirements.txt --upgrade

# 更新OpenClaw技能
openclaw skills update --all
```

### 日志监控
```bash
# 查看实时日志
openclaw logs --follow

# 查看错误日志
grep -i error /var/log/openclaw/*.log

# 清理旧日志
find /var/log/openclaw -name "*.log" -mtime +7 -delete
```

### 备份配置
```bash
# 备份OpenClaw配置
cp -r ~/.openclaw ~/.openclaw.backup.$(date +%Y%m%d)

# 备份项目配置
tar -czf config.backup.$(date +%Y%m%d).tar.gz config/
```

## 🎉 完成标志

### 成功部署的标志
1. ✅ OpenClaw服务在指定端口运行
2. ✅ 小红书发布助手技能可用
3. ✅ 能访问外部API（GitHub/Twitter）
4. ✅ 能连接ClawHub
5. ✅ 核心监控功能正常工作

### 快速验证命令
```bash
# 一键验证
./scripts/pipeline.py --all && \
openclaw skills list | grep xiaohongshu && \
curl -s https://api.github.com | grep -q "current_user_url" && \
echo "✅ 部署成功"
```

## 📞 故障排除

### 常见问题速查

#### Q1: OpenClaw服务启动失败
```bash
# 查看详细错误
openclaw gateway status --verbose

# 常见原因：
# 1. 端口被占用
# 2. 配置文件错误
# 3. 权限不足
```

#### Q2: 技能无法加载
```bash
# 查看技能日志
openclaw skills list --debug

# 常见原因：
# 1. 技能路径错误
# 2. 依赖缺失
# 3. 权限问题
```

#### Q3: 网络访问失败
```bash
# 测试网络
openclaw run --network-test true

# 常见原因：
# 1. 代理配置错误
# 2. 防火墙阻止
# 3. DNS问题
```

#### Q4: ClawHub连接失败
```bash
# 测试连接
curl -I https://clawhub.com

# 常见原因：
# 1. 网络问题
# 2. 证书问题
# 3. 源配置错误
```

### 获取帮助
```bash
# 查看OpenClaw帮助
openclaw --help
openclaw gateway --help
openclaw skills --help

# 查看文档
openclaw docs open

# 社区支持
# Discord: https://discord.com/invite/clawd
# GitHub: https://github.com/openclaw/openclaw
```

---
*部署指南版本: v2.0 (精准无冗余版)*  
*最后更新: 2026-02-26*  
*适用环境: Ubuntu/Debian, OpenClaw v2026.2+*